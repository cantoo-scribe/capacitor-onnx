import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cantoo.capacitoronnx.host',
  appName: 'Capacitor ONNX Host',
  webDir: 'dist',
};

export default config;
